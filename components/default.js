// Generated by CoffeeScript 1.9.1
var attending, component, dom, inject, ql, ref, widget;

ref = require('odojs'), component = ref.component, dom = ref.dom, widget = ref.widget;

inject = require('injectinto');

ql = require('odoql/ql');

attending = component({
  render: function(state, params) {
    var nameinput;
    nameinput = function() {
      return dom('div', [
        dom('input', {
          attributes: {
            type: 'text',
            autocomplete: 'off',
            autocorrect: 'off'
          }
        })
      ]);
    };
    return dom('div', [
      dom('div', [
        dom('label', [
          dom('input', {
            attributes: {
              type: 'checkbox'
            }
          }), dom('span'), dom('span', "RSVP to the " + params.title)
        ])
      ]), dom('h4', 'Who is attending?'), nameinput(), nameinput(), nameinput(), nameinput()
    ]);
  }
});

inject.bind('page:default', component({
  render: function(state, params) {
    var titileattr;
    titileattr = {
      attributes: {
        "class": 'title',
        src: '/title.png'
      }
    };
    return dom('div', {
      attributes: {
        "class": 'wrapper'
      }
    }, [
      dom('h1', 'Joe Bloggs'), dom('img', titileattr), attending({}, {
        title: 'Pre Wedding Celebrations'
      }), attending({}, {
        title: 'Wedding Ceremony'
      }), attending({}, {
        title: 'Wedding Reception'
      })
    ]);
  }
}));
